upstream auth_service {
    server localhost:4000;
}

upstream dashboard_service {
    server localhost:4001;
}

upstream store_service {
    server localhost:4002;
}

upstream main_app {
    server localhost:3000;
}

upstream dashboard_app {
    server localhost:3001;
}

upstream store_app {
    server localhost:3002;
}

server {
    listen 80;
    server_name app.myplatform.local;

    location / {
        proxy_pass http://main_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/ {
        proxy_pass http://auth_service/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cookie_domain ~^(.*)$ ".myplatform.local";
        proxy_cookie_path / /;
    }
}

server {
    listen 80;
    server_name dashboard.myplatform.local;

    location / {
        proxy_pass http://dashboard_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/ {
        proxy_pass http://dashboard_service/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cookie_domain ~^(.*)$ ".myplatform.local";
        proxy_cookie_path / /;
    }
}

server {
    listen 80;
    server_name store.myplatform.local;

    location / {
        proxy_pass http://store_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/ {
        proxy_pass http://store_service/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cookie_domain ~^(.*)$ ".myplatform.local";
        proxy_cookie_path / /;
    }
}
